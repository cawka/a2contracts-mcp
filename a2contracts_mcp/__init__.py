"""A2 Contracts MCP server -- see README.md."""

# The a2cons-contracts backend commit this server's tools were last
# written/verified against -- bump it whenever the tools change to match
# an API change (the two repos move together; the app has no API version
# number of its own). Shown by `a2contracts-mcp check`.
TRACKS_APP_COMMIT = '95498b6'
